# Siguiente sesión

Se sobrescribe cada vez; no se amplía. Lo que queda más allá de la próxima
sesión está en `pendiente.md`.

Escrito el 26 de septiembre de 2026, al cerrar la auditoría completa.

## Qué toca: abrir Expresión Gráfica

Es la última asignatura de esta tanda (CLAUDE.md §00) y la primera cuyo examen
es un dibujo. **Antes que contenido, diseño**: el sitio es estático y no sabe
corregir una vista ni un corte (§04, el paso `dibujar`).

1. **`npm run suelo` en verde sobre `main`** antes de tocar nada, solo, sin
   nada en paralelo.
2. **Inventariar el material** listando el directorio entero, no buscando por
   extensión (§17), mirando el final de cada PDF antes de usarlo y sin abrir
   los dos ficheros de notas (CLAUDE.md, «Antes de nada»).
3. **Leer el paquete de diseño** que ya existe fuera del repositorio
   (`Claude outputs/expresion-grafica-paquete.zip`) sin copiarlo: qué propone
   el paso `construir`, qué se puede hacer con `dibujar`, y si esto es por fin
   el patrón «figura fija» (§05).
4. **Temario oficial y evaluación con la guía citada**: el catálogo sale de
   `prev`.
5. **Los dos primeros temas enteros** antes de extraer ningún componente
   (§13: el patrón se destila del contenido).
